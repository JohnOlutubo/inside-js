function nextEdge(side1, side2) {
	const maxRangeOfThirdEdge = (side1 + side2) - 1;
  console.log(maxRangeOfThirdEdge);
}
nextEdge(4, 5);

function addition(num) {
	const newNum = num + 1 ;
	console.log(newNum);
}
addition(1)



const numbers = [1, 4, 6, 8, 0]

// enter code below this line
numbers[1] = 88

console.log(numbers)